---
title: Martin Costello's Blog
---

<div class="jumbotron">
    <h1><%= blog_title %></h1>
    <p class="lead"><%= blog_subtitle %></p>
</div>
<div class="row">
    <% article_count = blog.articles.length %>
    <% blog.articles[0..2].each do |article| %>
    <div class="col-md-<%= article_count == 1 ? "12" : (article_count == 2 ? "6" : "4") %>">
        <h2><%= link_to article.title, article %></h2>
        <p><%= article.date.strftime('%e %B %Y') %></p>
        <p><%= article.summary %></p>
        <p><a href="<%= article.url %>" alt="Read more" title="Read more">Read more...</a></p>
    </div>
    <% end %>
</div>
